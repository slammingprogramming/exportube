## What does this change?

<!-- One or two sentences. -->

## Why?

<!-- What problem does this solve, or what does it improve? -->

## Testing

- [ ] `pytest` passes locally
- [ ] Added/updated tests for this change
- [ ] Updated `AGENTS.md`/`docs/` if this changes architecture, config, or
      adds/replaces a provider

## Checklist

- [ ] No personal/real watch-history data, credentials, or tokens included
      in this diff (check `data/`, `output/`, `.env`, `secrets/` aren't
      accidentally staged)
- [ ] Scope is focused -- unrelated cleanups are in a separate PR
